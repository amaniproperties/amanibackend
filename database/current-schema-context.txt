-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.id_sequences (
  table_name text NOT NULL,
  prefix text NOT NULL,
  last_value bigint NOT NULL DEFAULT 0,
  CONSTRAINT id_sequences_pkey PRIMARY KEY (table_name)
);
CREATE TABLE public.content_sections (
  id text NOT NULL,
  label text NOT NULL,
  title text,
  subtitle text,
  body text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT content_sections_pkey PRIMARY KEY (id)
);
CREATE TABLE public.filters (
  id text NOT NULL,
  title text NOT NULL,
  location_label text,
  type_label text,
  price_label text,
  features_label text,
  apply_text text,
  min_price_label text,
  max_price_label text,
  bedrooms_label text,
  bathrooms_label text,
  area_label text,
  status_label text,
  sale_label text,
  rent_label text,
  lease_label text,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT filters_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_stats (
  id text NOT NULL,
  name text,
  bedrooms numeric,
  bathrooms text,
  sqft text,
  lot_size text,
  garage text,
  extra text,
  source_stats_id text UNIQUE,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_stats_pkey PRIMARY KEY (id)
);
CREATE TABLE public.site_navbars (
  id text NOT NULL,
  name text NOT NULL,
  brand_title text,
  brand_link text,
  brand_icon_src text,
  brand_icon_alt text,
  brand_icon_width integer,
  brand_icon_height integer,
  brand_icon_style text,
  cta_text text,
  cta_href text,
  root_classes text,
  nav_classes text,
  dropdown_menu_classes text,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT site_navbars_pkey PRIMARY KEY (id)
);
CREATE TABLE public.site_nav_items (
  id text NOT NULL,
  navbar_id text NOT NULL,
  parent_id text,
  item_type text NOT NULL,
  text text NOT NULL,
  href text,
  css_classes text,
  display_order integer NOT NULL DEFAULT 0,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT site_nav_items_pkey PRIMARY KEY (id),
  CONSTRAINT site_nav_items_navbar_id_fkey FOREIGN KEY (navbar_id) REFERENCES public.site_navbars(id),
  CONSTRAINT site_nav_items_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.site_nav_items(id)
);
CREATE TABLE public.site_footers (
  id text NOT NULL,
  name text NOT NULL,
  root_classes text,
  wow_delay text,
  inner_container_classes text,
  newsletter_title text,
  newsletter_text text,
  newsletter_placeholder text,
  newsletter_button_text text,
  newsletter_max_width_style text,
  copyright_left_html text,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT site_footers_pkey PRIMARY KEY (id)
);
CREATE TABLE public.footer_contact_items (
  id text NOT NULL,
  footer_id text NOT NULL,
  title text,
  address text,
  phone text,
  email text,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT footer_contact_items_pkey PRIMARY KEY (id),
  CONSTRAINT footer_contact_items_footer_id_fkey FOREIGN KEY (footer_id) REFERENCES public.site_footers(id)
);
CREATE TABLE public.footer_links (
  id text NOT NULL,
  footer_id text NOT NULL,
  link_group text NOT NULL,
  text text NOT NULL,
  href text,
  icon text,
  image_src text,
  display_order integer NOT NULL DEFAULT 0,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT footer_links_pkey PRIMARY KEY (id),
  CONSTRAINT footer_links_footer_id_fkey FOREIGN KEY (footer_id) REFERENCES public.site_footers(id)
);
CREATE TABLE public.service_points (
  id text NOT NULL,
  service_id text NOT NULL,
  point_text text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT service_points_pkey PRIMARY KEY (id)
);
CREATE TABLE public.service_stats (
  id text NOT NULL,
  service_id text NOT NULL,
  stat_number text NOT NULL,
  stat_label text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT service_stats_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_tabs (
  id text NOT NULL,
  source_tab_id text UNIQUE,
  tab_dom_id text NOT NULL,
  title text NOT NULL,
  href text,
  is_active boolean NOT NULL DEFAULT false,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_tabs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_media (
  id text NOT NULL,
  property_id text NOT NULL,
  media_group text NOT NULL DEFAULT 'gallery'::text,
  file_path text NOT NULL,
  alt_text text,
  display_order integer NOT NULL DEFAULT 0,
  is_primary boolean NOT NULL DEFAULT false,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_media_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_header_carousel_images (
  id text NOT NULL,
  property_header_id text NOT NULL,
  image_path text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_header_carousel_images_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_detail_paragraphs (
  id text NOT NULL,
  property_detail_id text NOT NULL,
  paragraph_text text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_detail_paragraphs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_detail_features (
  id text NOT NULL,
  property_detail_id text NOT NULL,
  feature_text text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  slug text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_detail_features_pkey PRIMARY KEY (id)
);
CREATE TABLE public.agents (
  id text NOT NULL DEFAULT make_prefixed_id('agen'::text, 'agents_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  slug text UNIQUE,
  name text NOT NULL,
  role text,
  location text,
  languages jsonb NOT NULL DEFAULT '[]'::jsonb,
  image text,
  status jsonb NOT NULL DEFAULT '[]'::jsonb,
  specialties jsonb NOT NULL DEFAULT '[]'::jsonb,
  quote text,
  stats_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  contact_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  socials_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  cta_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  avatar text,
  background_image text,
  availability text,
  bio text,
  mission text,
  timeline_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  office_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  achievements_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  metrics_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  service_areas_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  is_synthetic boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT agents_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_categories (
  id text NOT NULL DEFAULT make_prefixed_id('prop'::text, 'property_categories_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  slug text UNIQUE,
  title text NOT NULL,
  icon text,
  item_count integer,
  link text,
  delay text,
  is_synthetic boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_categories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.property_types (
  id text NOT NULL DEFAULT make_prefixed_id('prop'::text, 'property_types_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  slug text UNIQUE,
  title text NOT NULL,
  category_slug text,
  listing_type text,
  is_synthetic boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.features (
  id text NOT NULL DEFAULT make_prefixed_id('feat'::text, 'features_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  slug text UNIQUE,
  name text NOT NULL,
  is_synthetic boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT features_pkey PRIMARY KEY (id)
);
CREATE TABLE public.locations (
  id text NOT NULL DEFAULT make_prefixed_id('loca'::text, 'locations_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  parent_id text,
  slug text,
  name text NOT NULL,
  depth integer NOT NULL DEFAULT 0,
  lineage text,
  is_synthetic boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT locations_pkey PRIMARY KEY (id),
  CONSTRAINT locations_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.locations(id)
);
CREATE TABLE public.stats (
  id text NOT NULL DEFAULT make_prefixed_id('stat'::text, 'stats_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  slug text UNIQUE,
  bedrooms numeric,
  bathrooms_text text,
  sqft_text text,
  lot_size text,
  garage text,
  extra text,
  is_synthetic boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stats_pkey PRIMARY KEY (id)
);
CREATE TABLE public.services (
  id text NOT NULL DEFAULT make_prefixed_id('serv'::text, 'services_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  slug text UNIQUE,
  icon text,
  title text NOT NULL,
  description text,
  features_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  cta_text text,
  cta_href text,
  aos_delay integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT services_pkey PRIMARY KEY (id)
);
CREATE TABLE public.top_services (
  id text NOT NULL DEFAULT make_prefixed_id('tops'::text, 'top_services_seq'::regclass),
  legacy_id text NOT NULL UNIQUE,
  slug text UNIQUE,
  icon text,
  title text NOT NULL,
  description text,
  stats_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  cta_text text,
  cta_href text,
  image text,
  aos text,
  delay integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT top_services_pkey PRIMARY KEY (id)
);
CREATE TABLE public.properties (
  id text NOT NULL,
  slug text UNIQUE,
  title text NOT NULL,
  type_id text,
  status text,
  price_display text,
  rent_amount numeric,
  rent_period text,
  year_built integer,
  location_id text,
  stats_id text,
  agent_id text,
  financing_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  description_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  listed_date date,
  notes text,
  thumbnail text,
  featured boolean NOT NULL DEFAULT false,
  legacy_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  search_tsv tsvector,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  parent_property_id text,
  property_group_kind text NOT NULL DEFAULT 'single_unit'::text,
  structure_kind text,
  has_multiple_units boolean NOT NULL DEFAULT false,
  total_floors integer,
  total_units integer,
  unit_summary_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT properties_pkey PRIMARY KEY (id),
  CONSTRAINT properties_parent_property_id_fkey FOREIGN KEY (parent_property_id) REFERENCES public.properties(id),
  CONSTRAINT properties_type_id_fkey FOREIGN KEY (type_id) REFERENCES public.property_types(id),
  CONSTRAINT properties_location_id_fkey FOREIGN KEY (location_id) REFERENCES public.locations(id),
  CONSTRAINT properties_stats_id_fkey FOREIGN KEY (stats_id) REFERENCES public.stats(id),
  CONSTRAINT properties_agent_id_fkey FOREIGN KEY (agent_id) REFERENCES public.agents(id)
);
CREATE TABLE public.property_category_map (
  property_id text NOT NULL,
  category_id text NOT NULL,
  CONSTRAINT property_category_map_pkey PRIMARY KEY (property_id, category_id),
  CONSTRAINT property_category_map_property_id_fkey FOREIGN KEY (property_id) REFERENCES public.properties(id),
  CONSTRAINT property_category_map_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.property_categories(id)
);
CREATE TABLE public.property_feature_map (
  property_id text NOT NULL,
  feature_id text NOT NULL,
  CONSTRAINT property_feature_map_pkey PRIMARY KEY (property_id, feature_id),
  CONSTRAINT property_feature_map_property_id_fkey FOREIGN KEY (property_id) REFERENCES public.properties(id),
  CONSTRAINT property_feature_map_feature_id_fkey FOREIGN KEY (feature_id) REFERENCES public.features(id)
);
CREATE TABLE public.property_images (
  id text NOT NULL DEFAULT make_prefixed_id('prop'::text, 'property_images_seq'::regclass),
  property_id text NOT NULL,
  image_role text NOT NULL,
  file_path text NOT NULL,
  alt_text text,
  sort_order integer NOT NULL DEFAULT 1,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_images_pkey PRIMARY KEY (id),
  CONSTRAINT property_images_property_id_fkey FOREIGN KEY (property_id) REFERENCES public.properties(id)
);
CREATE TABLE public.property_details (
  property_id text NOT NULL,
  detail_slug text,
  title text,
  price_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  plot_size_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  map_embed text,
  gallery_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  description text,
  full_description_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  features_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  contact_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  extra_gallery_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  is_inferred boolean NOT NULL DEFAULT false,
  legacy_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_details_pkey PRIMARY KEY (property_id),
  CONSTRAINT property_details_property_id_fkey FOREIGN KEY (property_id) REFERENCES public.properties(id)
);
CREATE TABLE public.property_headers (
  property_id text NOT NULL,
  version text,
  container_class text,
  title text,
  highlight text,
  subtitle text,
  button_text text,
  button_link text,
  carousel_images_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  is_inferred boolean NOT NULL DEFAULT false,
  legacy_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_headers_pkey PRIMARY KEY (property_id),
  CONSTRAINT property_headers_property_id_fkey FOREIGN KEY (property_id) REFERENCES public.properties(id)
);
CREATE TABLE public.property_units (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('property_units'::text),
  legacy_id text UNIQUE,
  slug text NOT NULL UNIQUE,
  property_id text NOT NULL,
  unit_code text NOT NULL,
  unit_title text,
  unit_type text,
  block_name text,
  floor_no integer,
  bedrooms numeric,
  bathrooms numeric,
  toilets numeric,
  area_sqft numeric,
  area_label text,
  listing_type text CHECK (listing_type IS NULL OR (listing_type = ANY (ARRAY['rent'::text, 'sale'::text, 'lease'::text, 'internal'::text]))),
  rent_amount numeric,
  rent_period text,
  sale_amount numeric,
  deposit_amount numeric,
  service_charge_amount numeric,
  currency_code text DEFAULT 'KES'::text CHECK (currency_code ~ '^[A-Z]{3}$'::text),
  meter_no text,
  parking_slots integer,
  furnishing_status text,
  bathrooms_text text,
  kitchens integer,
  status text NOT NULL DEFAULT 'vacant'::text CHECK (status = ANY (ARRAY['vacant'::text, 'occupied'::text, 'reserved'::text, 'maintenance'::text, 'blocked'::text, 'inactive'::text])),
  availability_date date,
  available_for_public boolean NOT NULL DEFAULT true,
  is_listed boolean NOT NULL DEFAULT true,
  is_active boolean NOT NULL DEFAULT true,
  sort_order integer NOT NULL DEFAULT 0,
  features_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  attributes_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  notes text,
  internal_notes text,
  search_tsv tsvector,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  floor_id text,
  parent_unit_id text,
  unit_name text,
  unit_category text,
  usage_type text,
  office_rooms integer,
  square_feet numeric,
  square_meters numeric,
  listing_status text,
  occupancy_status text,
  pricing_model text,
  currency text,
  title text,
  short_description text,
  thumbnail text,
  is_primary boolean NOT NULL DEFAULT false,
  is_published boolean NOT NULL DEFAULT true,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  price_display text,
  CONSTRAINT property_units_pkey PRIMARY KEY (id),
  CONSTRAINT property_units_floor_id_fkey FOREIGN KEY (floor_id) REFERENCES public.property_floors(id),
  CONSTRAINT property_units_parent_unit_id_fkey FOREIGN KEY (parent_unit_id) REFERENCES public.property_units(id),
  CONSTRAINT property_units_property_id_fkey FOREIGN KEY (property_id) REFERENCES public.properties(id)
);
CREATE TABLE public.unit_tenancies (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('unit_tenancies'::text),
  legacy_id text UNIQUE,
  slug text NOT NULL UNIQUE,
  unit_id text NOT NULL,
  tenant_name text,
  tenant_phone text,
  tenant_email text,
  tenant_id_no text,
  occupant_count integer,
  employer_name text,
  emergency_contact_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  lease_start_date date,
  lease_end_date date,
  move_in_date date,
  move_out_date date,
  monthly_rent numeric,
  deposit_amount numeric,
  balance_amount numeric DEFAULT 0,
  billing_day integer CHECK (billing_day IS NULL OR billing_day >= 1 AND billing_day <= 31),
  payment_terms text,
  status text NOT NULL DEFAULT 'occupied'::text CHECK (status = ANY (ARRAY['draft'::text, 'reserved'::text, 'occupied'::text, 'vacated'::text, 'cancelled'::text, 'defaulted'::text])),
  is_primary_record boolean NOT NULL DEFAULT true,
  is_notice_given boolean NOT NULL DEFAULT false,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  notes text,
  internal_notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  first_name text,
  middle_name text,
  last_name text,
  full_name text,
  phone_secondary text,
  whatsapp_number text,
  date_of_birth date,
  gender text CHECK (gender IS NULL OR (gender = ANY (ARRAY['male'::text, 'female'::text, 'other'::text, 'prefer_not_to_say'::text]))),
  nationality text,
  occupation text,
  marital_status text,
  postal_address text,
  physical_address text,
  preferred_contact_method text CHECK (preferred_contact_method IS NULL OR (preferred_contact_method = ANY (ARRAY['phone'::text, 'email'::text, 'whatsapp'::text, 'sms'::text]))),
  id_document_type text,
  id_document_expiry_date date,
  employer_phone text,
  employer_address text,
  contract_status text CHECK (contract_status IS NULL OR (contract_status = ANY (ARRAY['draft'::text, 'pending'::text, 'active'::text, 'expired'::text, 'terminated'::text, 'cancelled'::text]))),
  contract_start_date date,
  contract_end_date date,
  contract_signed_date date,
  contract_sent_at timestamp with time zone,
  contract_email_sent boolean NOT NULL DEFAULT false,
  contract_whatsapp_sent boolean NOT NULL DEFAULT false,
  intended_move_out_date date,
  tenancy_source text,
  tenant_status text CHECK (tenant_status IS NULL OR (tenant_status = ANY (ARRAY['active'::text, 'inactive'::text, 'blacklisted'::text, 'former'::text]))),
  deposit_breakdown_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  billing_summary_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  primary_client_id text,
  CONSTRAINT unit_tenancies_pkey PRIMARY KEY (id),
  CONSTRAINT unit_tenancies_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT unit_tenancies_primary_client_id_fkey FOREIGN KEY (primary_client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.tenancy_notices (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_notices'::text),
  legacy_id text UNIQUE,
  slug text NOT NULL UNIQUE,
  tenancy_id text NOT NULL,
  notice_type text NOT NULL DEFAULT 'vacate'::text CHECK (notice_type = ANY (ARRAY['vacate'::text, 'renewal'::text, 'termination'::text, 'landlord_notice'::text, 'rent_review'::text])),
  notice_date date NOT NULL,
  effective_date date,
  vacate_date date,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['draft'::text, 'active'::text, 'withdrawn'::text, 'completed'::text, 'cancelled'::text])),
  reason text,
  remarks text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tenancy_notices_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_notices_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id)
);
CREATE TABLE public.unit_status_history (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('unit_status_history'::text),
  unit_id text NOT NULL,
  old_status text,
  new_status text NOT NULL CHECK (new_status = ANY (ARRAY['vacant'::text, 'occupied'::text, 'reserved'::text, 'maintenance'::text, 'blocked'::text, 'inactive'::text])),
  reason text,
  changed_by uuid,
  changed_at timestamp with time zone NOT NULL DEFAULT now(),
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT unit_status_history_pkey PRIMARY KEY (id),
  CONSTRAINT unit_status_history_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id)
);
CREATE TABLE public.property_unit_media (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('property_unit_media'::text),
  unit_id text NOT NULL,
  media_group text NOT NULL DEFAULT 'gallery'::text CHECK (media_group = ANY (ARRAY['gallery'::text, 'floorplan'::text, 'document'::text, 'video'::text, 'other'::text])),
  file_path text NOT NULL,
  alt_text text,
  display_order integer NOT NULL DEFAULT 0,
  is_primary boolean NOT NULL DEFAULT false,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  slug text,
  CONSTRAINT property_unit_media_pkey PRIMARY KEY (id),
  CONSTRAINT property_unit_media_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id)
);
CREATE TABLE public.property_floors (
  id text NOT NULL DEFAULT ('pflr-'::text || substr(replace((gen_random_uuid())::text, '-'::text, ''::text), 1, 12)),
  property_id text NOT NULL,
  slug text UNIQUE,
  floor_number integer NOT NULL,
  floor_code text NOT NULL,
  floor_label text NOT NULL,
  floor_category text NOT NULL DEFAULT 'residential'::text,
  square_feet numeric,
  notes text,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT property_floors_pkey PRIMARY KEY (id),
  CONSTRAINT property_floors_property_id_fkey FOREIGN KEY (property_id) REFERENCES public.properties(id)
);
CREATE TABLE public.tenant_next_of_kin (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenant_next_of_kin'::text),
  slug text UNIQUE,
  tenancy_id text NOT NULL,
  first_name text NOT NULL,
  middle_name text,
  last_name text NOT NULL,
  full_name text,
  relationship_to_tenant text NOT NULL,
  id_number text,
  phone_primary text NOT NULL,
  phone_secondary text,
  whatsapp_number text,
  email text,
  gender text,
  date_of_birth date,
  postal_address text,
  physical_address text,
  is_primary boolean NOT NULL DEFAULT false,
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  client_id text,
  CONSTRAINT tenant_next_of_kin_pkey PRIMARY KEY (id),
  CONSTRAINT tenant_next_of_kin_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT tenant_next_of_kin_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.unit_reservations (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('unit_reservations'::text),
  slug text UNIQUE,
  unit_id text NOT NULL,
  tenancy_id text,
  reserver_name text NOT NULL,
  reserver_phone text,
  reserver_email text,
  reserver_id_no text,
  reservation_date date NOT NULL DEFAULT CURRENT_DATE,
  expected_move_in_date date,
  reservation_expiry_date date,
  reservation_fee_amount numeric NOT NULL DEFAULT 0 CHECK (reservation_fee_amount >= 0::numeric),
  currency_code text NOT NULL DEFAULT 'KES'::text CHECK (currency_code ~ '^[A-Z]{3}$'::text),
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['draft'::text, 'active'::text, 'expired'::text, 'converted'::text, 'cancelled'::text])),
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  client_id text,
  CONSTRAINT unit_reservations_pkey PRIMARY KEY (id),
  CONSTRAINT unit_reservations_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT unit_reservations_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT unit_reservations_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.tenancy_contracts (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_contracts'::text),
  slug text UNIQUE,
  contract_number text UNIQUE,
  tenancy_id text NOT NULL,
  unit_id text NOT NULL,
  contract_template_name text,
  contract_version text,
  contract_status text NOT NULL DEFAULT 'draft'::text CHECK (contract_status = ANY (ARRAY['draft'::text, 'pending_approval'::text, 'active'::text, 'expired'::text, 'terminated'::text, 'cancelled'::text])),
  start_date date NOT NULL,
  end_date date NOT NULL,
  signed_date date,
  activation_date date,
  pdf_file_path text,
  email_sent boolean NOT NULL DEFAULT false,
  email_sent_at timestamp with time zone,
  whatsapp_sent boolean NOT NULL DEFAULT false,
  whatsapp_sent_at timestamp with time zone,
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  primary_client_id text,
  CONSTRAINT tenancy_contracts_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_contracts_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT tenancy_contracts_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT tenancy_contracts_primary_client_id_fkey FOREIGN KEY (primary_client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.tenancy_contract_delivery_log (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_contract_delivery_log'::text),
  contract_id text NOT NULL,
  delivery_channel text NOT NULL CHECK (delivery_channel = ANY (ARRAY['email'::text, 'whatsapp'::text, 'sms'::text, 'manual'::text])),
  recipient_value text NOT NULL,
  delivery_status text NOT NULL DEFAULT 'pending'::text CHECK (delivery_status = ANY (ARRAY['pending'::text, 'sent'::text, 'delivered'::text, 'failed'::text, 'cancelled'::text])),
  delivered_at timestamp with time zone,
  provider_name text,
  provider_message_id text,
  error_message text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tenancy_contract_delivery_log_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_contract_delivery_log_contract_id_fkey FOREIGN KEY (contract_id) REFERENCES public.tenancy_contracts(id)
);
CREATE TABLE public.tenancy_billing_accounts (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_billing_accounts'::text),
  tenancy_id text NOT NULL UNIQUE,
  unit_id text NOT NULL,
  currency_code text NOT NULL DEFAULT 'KES'::text CHECK (currency_code ~ '^[A-Z]{3}$'::text),
  billing_day integer CHECK (billing_day IS NULL OR billing_day >= 1 AND billing_day <= 31),
  account_status text NOT NULL DEFAULT 'active'::text CHECK (account_status = ANY (ARRAY['active'::text, 'suspended'::text, 'closed'::text])),
  opening_balance numeric NOT NULL DEFAULT 0,
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  primary_client_id text,
  CONSTRAINT tenancy_billing_accounts_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_billing_accounts_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT tenancy_billing_accounts_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT tenancy_billing_accounts_primary_client_id_fkey FOREIGN KEY (primary_client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.tenancy_charges (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_charges'::text),
  billing_account_id text NOT NULL,
  tenancy_id text NOT NULL,
  unit_id text NOT NULL,
  charge_type text NOT NULL CHECK (charge_type = ANY (ARRAY['rent'::text, 'rent_deposit'::text, 'electricity_deposit'::text, 'water_deposit'::text, 'garbage_fee'::text, 'service_charge'::text, 'utility_bill'::text, 'penalty'::text, 'repair'::text, 'other'::text])),
  charge_code text,
  description text,
  amount numeric NOT NULL CHECK (amount >= 0::numeric),
  currency_code text NOT NULL DEFAULT 'KES'::text CHECK (currency_code ~ '^[A-Z]{3}$'::text),
  billing_period_start date,
  billing_period_end date,
  charge_date date NOT NULL DEFAULT CURRENT_DATE,
  due_date date,
  status text NOT NULL DEFAULT 'posted'::text CHECK (status = ANY (ARRAY['draft'::text, 'posted'::text, 'partially_paid'::text, 'paid'::text, 'void'::text])),
  is_recurring boolean NOT NULL DEFAULT false,
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tenancy_charges_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_charges_billing_account_id_fkey FOREIGN KEY (billing_account_id) REFERENCES public.tenancy_billing_accounts(id),
  CONSTRAINT tenancy_charges_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT tenancy_charges_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id)
);
CREATE TABLE public.tenancy_payments (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_payments'::text),
  billing_account_id text NOT NULL,
  tenancy_id text NOT NULL,
  unit_id text NOT NULL,
  payment_date date NOT NULL DEFAULT CURRENT_DATE,
  amount numeric NOT NULL CHECK (amount > 0::numeric),
  currency_code text NOT NULL DEFAULT 'KES'::text CHECK (currency_code ~ '^[A-Z]{3}$'::text),
  payment_method text NOT NULL CHECK (payment_method = ANY (ARRAY['cash'::text, 'bank'::text, 'mpesa'::text, 'card'::text, 'cheque'::text, 'other'::text])),
  reference_no text,
  receipt_no text,
  status text NOT NULL DEFAULT 'received'::text CHECK (status = ANY (ARRAY['draft'::text, 'received'::text, 'reversed'::text, 'bounced'::text, 'cancelled'::text])),
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  client_id text,
  CONSTRAINT tenancy_payments_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_payments_billing_account_id_fkey FOREIGN KEY (billing_account_id) REFERENCES public.tenancy_billing_accounts(id),
  CONSTRAINT tenancy_payments_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT tenancy_payments_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT tenancy_payments_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.tenancy_payment_allocations (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_payment_allocations'::text),
  payment_id text NOT NULL,
  charge_id text NOT NULL,
  allocated_amount numeric NOT NULL CHECK (allocated_amount > 0::numeric),
  allocation_date date NOT NULL DEFAULT CURRENT_DATE,
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tenancy_payment_allocations_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_payment_allocations_payment_id_fkey FOREIGN KEY (payment_id) REFERENCES public.tenancy_payments(id),
  CONSTRAINT tenancy_payment_allocations_charge_id_fkey FOREIGN KEY (charge_id) REFERENCES public.tenancy_charges(id)
);
CREATE TABLE public.tenancy_events (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_events'::text),
  tenancy_id text,
  unit_id text,
  event_type text NOT NULL CHECK (event_type = ANY (ARRAY['reservation_created'::text, 'tenancy_created'::text, 'contract_generated'::text, 'contract_sent'::text, 'contract_signed'::text, 'moved_in'::text, 'notice_given'::text, 'notice_withdrawn'::text, 'rent_due'::text, 'rent_overdue'::text, 'payment_received'::text, 'payment_reversed'::text, 'move_out'::text, 'tenancy_closed'::text, 'other'::text])),
  event_date timestamp with time zone NOT NULL DEFAULT now(),
  title text,
  description text,
  severity text CHECK (severity IS NULL OR (severity = ANY (ARRAY['info'::text, 'warning'::text, 'critical'::text]))),
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  client_id text,
  CONSTRAINT tenancy_events_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_events_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT tenancy_events_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT tenancy_events_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.clients (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('clients'::text),
  slug text UNIQUE,
  client_code text UNIQUE,
  first_name text,
  middle_name text,
  last_name text,
  full_name text NOT NULL,
  id_number text,
  phone_primary text,
  phone_secondary text,
  whatsapp_number text,
  email text,
  date_of_birth date,
  gender text CHECK (gender IS NULL OR (gender = ANY (ARRAY['male'::text, 'female'::text, 'other'::text, 'prefer_not_to_say'::text]))),
  nationality text,
  occupation text,
  employer_name text,
  marital_status text,
  postal_address text,
  physical_address text,
  preferred_contact_method text CHECK (preferred_contact_method IS NULL OR (preferred_contact_method = ANY (ARRAY['phone'::text, 'email'::text, 'whatsapp'::text, 'sms'::text]))),
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'inactive'::text, 'blacklisted'::text, 'former'::text, 'deceased'::text])),
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT clients_pkey PRIMARY KEY (id)
);
CREATE TABLE public.client_documents (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('client_documents'::text),
  slug text UNIQUE,
  client_id text NOT NULL,
  document_type text NOT NULL CHECK (document_type = ANY (ARRAY['national_id'::text, 'passport'::text, 'kra_pin'::text, 'photo'::text, 'signed_contract'::text, 'proof_of_income'::text, 'bank_statement'::text, 'utility_bill'::text, 'other'::text])),
  document_title text,
  file_path text NOT NULL,
  file_mime_type text,
  file_size_bytes bigint,
  issue_date date,
  expiry_date date,
  is_verified boolean NOT NULL DEFAULT false,
  verified_at timestamp with time zone,
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT client_documents_pkey PRIMARY KEY (id),
  CONSTRAINT client_documents_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.tenancy_clients (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('tenancy_clients'::text),
  tenancy_id text NOT NULL,
  client_id text NOT NULL,
  role text NOT NULL DEFAULT 'primary_tenant'::text CHECK (role = ANY (ARRAY['primary_tenant'::text, 'co_tenant'::text, 'occupant'::text, 'guarantor'::text, 'agent'::text])),
  is_primary boolean NOT NULL DEFAULT false,
  start_date date,
  end_date date,
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tenancy_clients_pkey PRIMARY KEY (id),
  CONSTRAINT tenancy_clients_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT tenancy_clients_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.contract_delivery_queue (
  id text NOT NULL DEFAULT make_prefixed_id_from_table('contract_delivery_queue'::text),
  contract_id text NOT NULL,
  tenancy_id text NOT NULL,
  client_id text,
  channel text NOT NULL CHECK (channel = ANY (ARRAY['email'::text, 'whatsapp'::text])),
  recipient_value text NOT NULL,
  queue_status text NOT NULL DEFAULT 'pending'::text CHECK (queue_status = ANY (ARRAY['pending'::text, 'processing'::text, 'sent'::text, 'failed'::text, 'cancelled'::text])),
  attempts integer NOT NULL DEFAULT 0,
  last_attempt_at timestamp with time zone,
  processed_at timestamp with time zone,
  error_message text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT contract_delivery_queue_pkey PRIMARY KEY (id),
  CONSTRAINT contract_delivery_queue_contract_id_fkey FOREIGN KEY (contract_id) REFERENCES public.tenancy_contracts(id),
  CONSTRAINT contract_delivery_queue_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT contract_delivery_queue_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.unit_utility_readings (
  id text NOT NULL DEFAULT ('uur-'::text || substr(replace((gen_random_uuid())::text, '-'::text, ''::text), 1, 16)),
  unit_id text NOT NULL,
  tenancy_id text,
  utility_type text NOT NULL CHECK (utility_type = ANY (ARRAY['water'::text, 'electricity'::text])),
  meter_no text,
  previous_reading numeric NOT NULL DEFAULT 0,
  current_reading numeric NOT NULL DEFAULT 0,
  units_used numeric NOT NULL DEFAULT 0,
  rate_amount numeric NOT NULL DEFAULT 0,
  bill_amount numeric NOT NULL DEFAULT 0,
  reading_date date NOT NULL DEFAULT CURRENT_DATE,
  billing_period_start date,
  billing_period_end date,
  status text NOT NULL DEFAULT 'billed'::text CHECK (status = ANY (ARRAY['draft'::text, 'billed'::text, 'paid'::text, 'void'::text])),
  notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT unit_utility_readings_pkey PRIMARY KEY (id),
  CONSTRAINT unit_utility_readings_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT unit_utility_readings_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id)
);
CREATE TABLE public.unit_maintenance_tickets (
  id text NOT NULL DEFAULT ('umt-'::text || substr(replace((gen_random_uuid())::text, '-'::text, ''::text), 1, 16)),
  unit_id text NOT NULL,
  tenancy_id text,
  client_id text,
  ticket_number text UNIQUE,
  issue_type text NOT NULL,
  priority text NOT NULL DEFAULT 'medium'::text CHECK (priority = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text, 'urgent'::text])),
  status text NOT NULL DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'assigned'::text, 'in_progress'::text, 'completed'::text, 'cancelled'::text])),
  reported_date timestamp with time zone NOT NULL DEFAULT now(),
  assigned_to text,
  assigned_phone text,
  scheduled_date date,
  completed_at timestamp with time zone,
  estimated_cost numeric DEFAULT 0,
  actual_cost numeric DEFAULT 0,
  description text,
  resolution_notes text,
  metadata_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT unit_maintenance_tickets_pkey PRIMARY KEY (id),
  CONSTRAINT unit_maintenance_tickets_unit_id_fkey FOREIGN KEY (unit_id) REFERENCES public.property_units(id),
  CONSTRAINT unit_maintenance_tickets_tenancy_id_fkey FOREIGN KEY (tenancy_id) REFERENCES public.unit_tenancies(id),
  CONSTRAINT unit_maintenance_tickets_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);